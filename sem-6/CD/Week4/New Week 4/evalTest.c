int sum(int x, int y)
{
    int a[10], b[10], i, j;
    printf("User defined functions\n");
    scanf("%i %i", &x, &y);
    for( i = 10; i < a[i]; i--)
    {
        for (j = 10; j < b[i]; j--)
        {
            return (x + y);
        }
    }
}

int main()
{
    int a, b, num1[10];
    char c, d;

    int num2[20];
    if (a <= b) {
        a = num1[10];
    }
    else
    {
        num2[0] = b;
    }

    while (num1[0] != 0)
    {
        printf("num = %i", a);
    }
}